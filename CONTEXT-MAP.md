# Context Map

One row per context that has grown a `CONTEXT.md` -- see
[Domain Docs](./docs/agents/domain.md). `crates/`, `packages/` and `apps/` are
modelled separately; none of them has its own glossary yet.

- **Root** ([CONTEXT.md](./CONTEXT.md)) -- cross-context terms only: the
  catalogs, the manifests they govern, and the release bumper.
