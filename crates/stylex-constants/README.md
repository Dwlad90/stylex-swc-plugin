# `StyleX Constants`

## Overview

This package provides constants, regex patterns and static data for the StyleX
SWC plugin. Includes CSS property orders, priorities, shorthands, error messages
and compiled regex patterns.
