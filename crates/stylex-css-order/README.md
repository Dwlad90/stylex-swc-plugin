# `StyleX CSS Order`

## Overview

CSS property ordering and specificity for the StyleX system.

Manages the deterministic ordering of CSS properties and specificity
rules within the StyleX NAPI-RS compiler.
