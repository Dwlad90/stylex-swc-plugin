# `StyleX CSS`

## Overview

This package provides CSS generation, normalization and validation for the
StyleX SWC plugin. Includes class name generation, LTR/RTL support, shorthand
expansion, CSS property validation and style merging.
