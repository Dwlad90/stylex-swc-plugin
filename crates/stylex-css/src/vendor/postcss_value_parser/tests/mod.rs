//! Tests for the vendored value parser.
//!
//! Split by what they answer. [`parity`] asks whether this agrees with the
//! JavaScript it stands in for, over a table generated by running that
//! JavaScript; it is the test that matters, and no expectation in it was
//! written by hand.
//! [`properties`] asks the questions a table cannot: that no input crashes the
//! parser, that the invariants the normalizers rely on hold for inputs too
//! large to tabulate, and that the walk order is what a caller is promised.

mod cases;
mod dump;
mod parity;
mod properties;
