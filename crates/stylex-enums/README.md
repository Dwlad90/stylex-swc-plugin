# `StyleX Enums`

## Overview

Enum definitions for the StyleX system.

Provides shared enum types used throughout the StyleX NAPI-RS compiler
crates for type-safe pattern matching and configuration.
