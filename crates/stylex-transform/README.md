# `StyleX Transform`

## Overview

This package provides the SWC Fold implementation and transform orchestration
for the StyleX compiler. Includes the `StyleXTransform` struct, `StateManager`
and all fold method implementations.
