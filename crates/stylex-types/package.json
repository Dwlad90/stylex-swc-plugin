{
  "name": "@stylexswc/types",
  "description": "Type definitions, enums, structs and traits for StyleX SWC plugin",
  "version": "0.18.5",
  "private": true,
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "bench": "scripty",
    "clean": "del-cli dist",
    "do:nothing": "exit 0",
    "format": "run-p format:rs format:toml",
    "format:check": "run-p format:rs:check format:toml:check",
    "format:rs": "cargo fmt --all",
    "format:rs:check": "cargo fmt -- --check",
    "format:toml": "taplo format",
    "format:toml:check": "pnpm run format:toml --check",
    "prepublishOnly": "pnpm run build",
    "test": "scripty",
    "test:coverage": "scripty",
    "test:flamegraph": "scripty",
    "test:profile": "NODE_ENV=test cargo flamegraph --root --test"
  },
  "config": {
    "scripty": {
      "path": "../../scripts/packages"
    }
  },
  "devDependencies": {
    "@stylexswc/structures": "catalog:internal",
    "@stylexswc/utils": "catalog:internal",
    "@taplo/cli": "catalog:tooling",
    "npm-run-all2": "catalog:tooling"
  },
  "keywords": [
    "stylex",
    "swc",
    "swc-plugin"
  ]
}
