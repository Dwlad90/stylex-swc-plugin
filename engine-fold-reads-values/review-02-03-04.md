# Code review — tickets 02, 03, 04

Reviewed: the working tree against `e669e02ab` (`git diff HEAD`, 13 files,
+1134/-185). Two axes, run as independent sub-agents so neither could rerank the
other: **Standards** (does it follow this repo's documented standards, plus the
Fowler smell baseline) and **Spec** (does it match what the tickets asked for).

Status: reviewed, findings triaged below. Every row marked **fixed** was applied
before the change was committed.

## Standards

| # | Finding | Verdict |
| --- | --- | --- |
| S1 | `CONTEXT.md:114` is 95 chars against the 80-char markdown limit. `lint:markdown` does not check width. | **fixed** — re-wrapped |
| S2 | Six new `pub fn`s in `stylex-constants/…/evaluation_errors.rs` with no test. That crate is *not* excluded from `test:coverage:workspace`, which runs `--fail-uncovered-lines 0`; its only caller, `stylex_transform`, *is* excluded, so nothing covers them. | **fixed** — one test per message |
| S3 | Deliberately unreachable arms (symbol key, `describe`'s symbol/BigInt, `to_expr`'s catch-all) read as Speculative Generality. | **fixed** — `describe` is deleted. The engine's own `typeof` names the kind, so there are no per-kind branches here to be unreachable, and a kind nobody thought of is named rather than falling into a catch-all. The message was rephrased to need no article so `typeof` passes straight through. The two remaining arms are exhaustive matches the crate's error policy requires. |
| S4 | Duplicated Code + Data Clumps: `Guard::descend` and `Outward::descend` are the same six lines, and both types carry `levels_left` beside one payload field. | **fixed** — one `Depth` newtype owns the bound and the refusal |
| S5 | Duplicated Code in the test harness: `fold` and `module` copied into three new files. | **fixed** — `fold_module` and `base_style_module` moved to `tests/utils`, and `evaluation_depth_budget.rs`'s pre-existing copy of `fold` now uses the shared one too |
| S6 | Mysterious Name: `to_expr` no longer converts a JS value; `read_length` reports the length bound for a length that is not a number. | **fixed** — renamed, and the two cases answer separately |
| S7 | Middle Man: `read<T>` is a one-line `map_err`, taking `FnMut` where `FnOnce` suffices. | **partly fixed** — `FnOnce`; the wrapper stays, since it is the one place a throw on the way out is worded |
| S8 | Glossary drift: the glossary names one concept, *refusal*; the code carries `Refusal` and `Decline`. | **fixed** — the pairing is stated where both are declared |

## Spec

| # | Finding | Verdict |
| --- | --- | --- |
| P1 | Ticket 04's checkbox "The existing fold tests pass unchanged, since no input changes shape" is ticked but untrue: four pinned inputs changed verdict. | **fixed** — the checkbox is corrected and the four are listed in the Answer |
| P2 | `object_size_too_large` is pinned only at the evaluator seam, where the other seven rules are pinned at the transform seam. | **fixed** — the refusal is pinned at the transform seam with the other seven. A generated source string is fine there; the evaluator seam keeps only the half with no sentence to assert, that the bound itself still folds |
| P3 | No evidence the parity harness was re-run. | **not a finding** — it was run: 1123 subjects, 0 changed, 0 unexpected, and the four rows were read individually. The numbers are now in the ticket Answers, which is what made it invisible |
| P4 | Scope: the outward nesting bound is in no 02/03/04 checkbox — nesting is ticket 11's. | **kept, handed on** — it stops a process abort rather than adding a feature, and ticket 11 now records that it has a second site to unify |
| P5 | Scope: `read()`/`read_length` add getter-throw and unreadable-length refusals with no ticket line. | **kept** — they are the outward half of ticket 02's "there is no third state"; a read that cannot answer has to say so |
| P6 | Scope: two corpus rows for ticket 04, where the corpus is ticket 14's. | **kept** — ticket 03 asks for its own row to be re-measured, and a row that records new agreement is what stops ticket 14 re-deriving it |
| P7 | **Rules fire before candidacy.** `admit_call` ran the locale, numeric-receiver and amplification checks before `admit_value(obj)`, so a call the guard would never fold answered `Rule` instead of `NotACandidate` and stopped falling through to the older dispatch. Latent today; live once 05–09 widen the surface. | **fixed** — candidacy is decided first. Costs nothing today, because the walk is still pure syntax; ticket 05 owns the re-split once the walk resolves bindings, and now says so |
| P8 | Depth likewise: `admit_value` descends before candidacy. | **correct, and my first answer was wrong.** I argued the ceilings match; they do not once a project raises `maxEvaluationDepth`, and measured, `"a".concat(<40 levels of &&>)` at a ceiling of 512 folded before this change and refuses after. Handing it back instead is *worse*, not better: the older path aborts on a nested array reaching its `join`, via a `stylex_panic!` older than this effort. The refusal stays, the wrong reasoning is replaced with the measurement, both halves are pinned, and ticket 11 is told to fix that panic before unifying the ceilings |
| P9 | `read_length` names a bound that did not fire for a non-numeric length. | **fixed** — same as S6 |

## Corrections to this review's own first pass

Two of the verdicts above were revised after being challenged. **P8** was
answered with an argument that measurement then contradicted — the fix stayed,
but for a different and better reason, and the code comment that carried the
wrong reason was replaced. **S3** was waved off as unavoidable and was not: the
question the cascade answered is one the engine already answers.

The pattern worth keeping: both were findings I first closed by explaining
rather than by measuring.

## What the two axes agreed on

`read_length`'s wrong sentence (S6/P9) was the only finding both axes reached
independently, from opposite directions — one as a naming smell, the other as a
rule that did not fire. It was the smallest defect and the most certain.
