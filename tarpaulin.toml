[default]
# Workspace settings
workspace = false              # Enable workspace mode to include all crates in the workspace
all-features = true           # Ensure all conditionally compiled code behind features is tested
release = false               # Run in debug mode (default, better for coverage mapping)

# Coverage Engine
engine = "Llvm"               # LLVM is significantly more accurate and faster than the default ptrace engine

# Exclusions & Ignored Code
ignore-tests = true           # Don't count the test functions themselves in the coverage metric
ignore-panics = true          # Ignore panic/unwind branches (crucial for hitting 100% in Rust)

exclude-files = [
    "**/build.rs",
    "**/tests/**",
    "**/benches/**",
    "**/examples/**",
    "**/stylex-test-parser/**",
    "**/stylex-rs-compiler/**",
    "**/.DS_Store",
    "crates/stylex-test-parser/**",
    "crates/stylex-rs-compiler/**",
    "**/crates/stylex-js/**",
    "**/crates/stylex-logs/**",
    # stylex-css-parser: keep only the fully covered transform property entrypoint.
    "**/crates/stylex-css-parser/src/at_queries/**",
    "**/crates/stylex-css-parser/src/base_types.rs",
    "**/crates/stylex-css-parser/src/css_types/**",
    "**/crates/stylex-css-parser/src/css_value.rs",
    "**/crates/stylex-css-parser/src/properties/border_radius.rs",
    "**/crates/stylex-css-parser/src/properties/box_shadow.rs",
    "**/crates/stylex-css-parser/src/properties/mod.rs",
    "**/crates/stylex-css-parser/src/token_parser.rs",
    "**/crates/stylex-css-parser/src/token_types.rs",
    # stylex-transform: exclude hard-to-instrument deep transformation flows.
    "**/crates/stylex-transform/src/shared/**",
    "**/crates/stylex-transform/src/transform/fold/**",
    "**/crates/stylex-transform/src/transform/mod.rs",
    "**/crates/stylex-transform/src/transform/styleq/**",
    "**/crates/stylex-transform/src/transform/stylex/**",
]

# Output settings
out = ["Html", "Lcov"]        # Generate an HTML report for humans and Lcov for CI tools (like Codecov)
output-dir = "target/tarpaulin"
target-dir = "/tmp/stylex-swc-plugin-tarpaulin-target"

# Enforcement
fail-under = 100              # Fail the CI/run if coverage drops below 100%

# General run settings
timeout = '300s'                 # Give tests up to 5 minutes to complete to prevent hanging
color = "Always"

# Run types: specify what should be executed
# 'Tests' covers unit/integration tests
# 'Doctests' covers examples in your doc comments (///)
run-types = ["Tests", "Doctests"]

# Explicitly enable documentation test coverage
doc = true
